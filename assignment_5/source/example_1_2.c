/*
bool window_class()
{
    HWND hOlly = FindWindow(_T("OLLYDBG"), NULL);
    HWND hIDA = FindWindow(_T("QWidget"), NULL);
    HWND hDbg = FindWindow(_T("WinDbgFrameClass"), NULL);
    HWND hIm = FindWindow(_T("Im"), NULL);

    if (hOlly || hIDA || hDbg || hIm)
    {
        return TRUE;
    }

    return FALSE;
}
*/